#include <stdio.h>
#include <stdlib.h>

/**
 *main - where the arg will be taken
 *@c: a int variable
 *@argv: the array of the args
 *Return: always to 0.
 */

int main(int c, char *argv[])
{
	c = 0;

	printf("%s\n", argv[c]);

	return (0);
}
